# Focus Atlas — frontend

React (Vite) 프론트엔드. 루트 README와 `docs/DESIGN_SYSTEM.md`를 참고.

```bash
npm install
npm run dev      # http://localhost:5173 (백엔드: backend/에서 python main.py)
npm test         # vitest
npm run build    # 프로덕션 번들 (VITE_API_BASE_URL 필요)
```
